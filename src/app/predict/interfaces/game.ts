export interface Game {
	gameMode: string; // Assuming this could be a string literal type if known values are available
	gameTime: number;
	mapName: string;
	mapNumber: number;
	mapTerrain: string; // Assuming this could also be a string literal type if known values are available
}
